# Tareas — finances-frontend

> Kanban del mundo. Editar desde el dashboard (`_shared/token-stack/dashboard.html`)
> o con `/tasks` en Claude Code — misma fuente, este archivo es la verdad.
>
> Toda tarea nueva lleva marca `[P]`/`[S]` junto al tag de módulo (ver
> `$AI_OS_ROOT/_shared/CONVENTIONS.md`):
> - `[P]` paralelizable — no toca los mismos archivos ni depende de otra tarea activa.
> - `[S]` secuencial — depende de otra tarea o toca archivos compartidos con ella.
>
> Formato: `- [ ] [P] [modulo][id] **Título** — descripción. (fecha)`

## backlog

## doing

## review

- [ ] [S] [ops][019] **PR #3 pendiente review compañero** — restaura entrada task #018 (pm2 local) perdida en conflicto stash vs main. https://github.com/Ledder-Dev/finances-frontend/pull/3 (2026-08-18)

## done

- [x] [S] [react][035] **Fix Hero banner: no cargaba al inicio + datos incorrectos** — root cause: `heroStats` se llenaba fragmentado por 3 tabs (`AnalysisTab`/`SavingsTab`/`DebtsTab`), cada uno seteando su porción solo al activarse (patrón "fetch on activate" de `.claude/CONVENTIONS.md` #4) — Hero nunca corría en carga inicial, y aun con click manual en un tab, mostraba datos incompletos porque dependía de que los otros dos también se hubieran visitado. Fix: `useEffect` centralizado en `AppStateProvider` (`src/state/AppStateContext.jsx`), dispara en mount + cambio de `currentMonth`, hace fetch paralelo a `/api/analysis`+`/api/savings`+`/api/debts` y calcula el shape completo de `heroStats` en un solo lugar, reusando exacta la lógica de cálculo que tenía cada tab. Excepción deliberada a la convención #4 (fetch-on-activate) — justificada porque `heroStats` es estado compartido/global, no propio de un tab, así que su fetch pertenece al dueño del estado (provider), no a un tab. Removida lógica `setHeroStats` fragmentada de los 3 tabs (`AnalysisTab.jsx`/`SavingsTab.jsx`/`DebtsTab.jsx`), cada `load()` simplificado a solo su propio fetch de UI. `DebtsTab.test.jsx` actualizado — mock de `apiFetch` ahora responde también a `/api/analysis`/`/api/savings` (nuevas llamadas que dispara `AppStateProvider` al montar). De paso, a pedido explícito del usuario: `api.test.js` hermetizado — 2 tests dependían del valor default `3001` en `API_BASE`, pero `.env.local` local (gitignored) trae `VITE_API_PORT=3002`, que `vi.unstubAllEnvs()` no limpia (solo limpia stubs explícitos, no valores de `.env.local` cargados por Vite). Agregado `vi.stubEnv('VITE_API_PORT', '3001')` explícito en ambos tests — ya no depende del entorno local del desarrollador. Verificado: `npm run build` limpio, `npm test -- --run` → 22/22 (antes 20/22, ambos fallos preexistentes de puerto ahora corregidos). No se pudo probar render interactivo en browser (sin herramienta de browser en este entorno) — solo build + lógica revisada. (2026-08-26)
- [x] [S] [ops][021] **Fix crash-loop pm2 `finances-frontend` (parpadeo pantalla)** — proceso pm2 en 1047 restarts, status stopped, causaba apertura/cierre visible de ventana. Causa raíz: `node_modules` sin `@vitejs/plugin-react` (declarado en `package.json`, no instalado) — `vite build`/dev fallaba en cada arranque, pm2 reintentaba sin parar. `npm install` reinstaló dependencias, `pm2 restart finances-frontend` → online estable, contador de restarts no vuelve a subir. Detectado a partir de revisión cruzada en `finances-api` corriendo en paralelo bajo pm2. (2026-08-18)
- [x] [S] [ops][020] **Sync rama `development` con `develop`** — `development` local/remota estaba 19 commits atrás de `origin/develop` (pre-migración React vs post-migración), pero era ancestro directo — fast-forward limpio sin conflictos. `git checkout development && git merge --ff-only origin/develop` + `git push origin development`. Distinto de `origin/develop` vs `origin/master`, que sigue 23 commits atrás (sin tocar, fuera de alcance de esta sesión). (2026-08-18)

- [x] [S] [ops][011] **Definir mecanismo de deploy pa finances-frontend** — decidido: Cloudflare Pages. Sitio estático sin router client-side (tabs por state en memoria), no requiere `_redirects`/SPA-fallback — setup puro por dashboard: build command `npm run build`, output `dist`, variable de entorno `VITE_API_BASE_URL`. Documentado en `README.md` sección Deploy. GitHub Pages sigue descartado (ver nota previa de esta entrada / `docs/adr/003-migracion-ledder-dev-github-pages.md`). (2026-08-13)
- [x] [S] [react][034] **Cutover** — eliminados `src/core.js`/`auth.js`/`debts.js`/`family-bank.js`/`hero.js`/`main.js`/`receipts.js`/`recurring.js`/`savings.js`/`settings.js`/`ui-helpers.js`/`ui-helpers.test.js` (vanilla viejo). Mantenidos porque siguen usados por React: `src/api.js`, `src/state.js` (`DEFAULT_PRODUCT_CATEGORIES`/`UNITS`/`txDefaultCategories`, sin cambios) + sus tests. `src/analysis.js` NO se mantuvo tal cual — primer intento del cutover lo dejó intacto asumiendo que React lo reusaba entero; en realidad React (`AnalysisTab.jsx`, #031) solo importa `trimmedMean`, el resto del archivo era markup/DOM vanilla muerto con imports rotos a `./hero.js`/`./core.js` (ambos eliminados), rompía `npm run build` (`Could not resolve "./hero.js" from "src/analysis.js"`). Corregido: `analysis.js` recortado a solo `trimmedMean` (sin import de `state.js`/`hero.js`/`core.js`), `analysis.test.js` sin cambios (ya solo testeaba `trimmedMean`). Grep de imports rotos hacia todos los módulos vanilla eliminados confirmado limpio en `src/` tras el recorte. `index.html` reemplazado por el contenido de `react-app.html` (retitulado "Finance Tracker"), `react-app.html` eliminado. `vite.config.js`: retirado `build.rollupOptions.input` de doble entrada (multi-page vanilla+React) y el import de `node:path` que ya no hacía falta, vuelto a entry point único default. Actualizados `CLAUDE.md` (sección Estado actual), `.claude/ARCHITECTURE.md` (visión general + decisiones, ya sin lenguaje de "migración en curso"), `.claude/CONVENTIONS.md` (sección 7 marcada histórica), `.claude/CURRENT_STATUS.md` (reescrito, refleja cutover completo), `README.md` (reescritura completa: stack React, estructura `src/` por dominio, testing con RTL, deploy Cloudflare Pages), `$AI_OS_ROOT/_shared/memory/registry.json` (stack + descripción del mundo actualizados). **Desviación explícita del alcance original de esta task, por instrucción directa del usuario:** NO se mergeó `feature/react-migration` → `develop`/`master` — la branch feature queda como está, el merge se excluye a propósito. **#033 (QA manual) saltada** — dependencia declarada de esta task, decisión de riesgo aceptado por el usuario (no completada, no ignorada silenciosamente: ver entrada de #033 abajo). Verificado: `npm run build` limpio, `npm test -- --run` → 22/22 (confirmado corriendo también sin `.env.local` local, gitignored, que en este entorno trae `VITE_API_PORT=3002` y hace fallar 2 tests de `api.test.js` que asumen el default `3001` — no es regresión de código, es override local esperado, mismo patrón preexistente documentado en tasks previas). (2026-08-13)
- [x] [S] [react][033] **QA manual end-to-end** — saltada deliberadamente por decisión del usuario (riesgo aceptado, no bloquear cutover #034 en ausencia de herramienta de browser en este entorno). No se ejecutó smoke test en navegador real de los 11 dominios contra `finances-api`. Queda como gap conocido: ningún dominio React fue probado con render/interacción real todavía — primer smoke test real pendiente de que el usuario lo corra manualmente cuando pueda. (2026-08-13)
- [x] [S] [react][032] **Tests** — `@testing-library/react`+`@testing-library/jest-dom` instalados (`npm audit fix` limpió una vuln transitiva de `nanoid` de paso). `analysis.test.js`/`api.test.js` NO se duplican — sus módulos (`trimmedMean` de `analysis.js`, `apiFetch`/`API_BASE` de `api.js`) son reusados tal cual por el lado React (no reimplementados, ver #031/#019), los 17 tests existentes ya cubren ese código compartido. `ui-helpers.test.js` SÍ requería puerto real — sus DOM-string-builders (`categoryInput`/`unitOptions`/`typeInputHtml`) fueron reemplazados por componentes en #021: `src/components/CategorySelect.test.jsx`, `UnitSelect.test.jsx`, `ProductTypeSelect.test.jsx` (mismos casos: opciones generadas, `value` seleccionado marcado, dedupe+sort de tipos, modo "New type" — vía RTL `render`/`fireEvent` en vez de parseo de HTML string), `src/lib/findProduct.test.js` (mismos 2 casos que la versión vieja, adaptados a la firma `findProduct(products, name)` de #021 en vez de leer `state.allProducts` global). Componente crítico agregado: `src/debts/DebtsTab.test.jsx` (#029) — carga+lista con total abierto, validación person/amount requeridos, `confirm()` bloqueando delete, `settle` disparando `PUT`; mock de `apiFetch` vía `vi.mock('../api.js')` en vez de stub de `window.fetch`, evita pelear con el orden de captura de `_fetch` en el módulo real. Gotcha encontrado y corregido: vitest no expone `afterEach`/`describe`/`it` como globals (no `test.globals:true` en `vite.config.js`) — el auto-cleanup de RTL depende de detectar un `afterEach` global, así que sin fix el DOM se acumulaba entre tests del mismo archivo (`Found multiple elements with role "combobox"`); agregado `src/test-setup.js` con `afterEach(cleanup)` explícito + `@testing-library/jest-dom/vitest`, registrado en `vite.config.js` (`test.setupFiles`). Verificado: `npm run build` limpio, `npm test -- --run` → 28/30 tests pasan, 8 archivos (2 fallos son los mismos preexistentes de `.env.local`/puerto, no nuevos). (2026-08-13)
- [x] [P] [react][031] **Dashboard Analysis + Hero** — Chart.js: descartado `react-chartjs-2` (paquete nuevo pa envolver un global que ya está cargado por CDN en `react-app.html`/`index.html` desde #018 — instalarlo duplicaría Chart.js vía npm bundle + CDN global, sin necesidad real). Usado `useRef` imperativo directo sobre `Chart` global, mismo patrón que vanilla (`state.chartInstances` → `chartInstances.current`), destroy-antes-de-recrear en cada cambio de `months` + cleanup en unmount. `src/analysis/AnalysisTab.jsx` (porta `analysisChartsCard`/`analysisTable`/`analysisAvgCard`/`analysisCatCard`/`addHistoricalMonth()`/`editAnalysisMonth()`/`saveAnalysisMonth()`/`deleteHistoricalMonth()` de `analysis.js` — `trimmedMean` reusada tal cual desde `analysis.js` en vez de duplicarla, sigue siendo función pura sin DOM; edit-inline de mes con state local en vez de `getElementById`; `deleteHistoricalMonth` SÍ tiene `confirm()`, preservado exacto). `src/dashboard/HeroBanner.jsx` + `src/dashboard/SummaryGrid.jsx` nuevos — no forman parte de `analysis.js`/`hero.js` (son de `core.js`/markup fuera de cualquier tab-section) pero sin ellos el Hero banner nunca aparece (`heroBanner` oculto entero si no hay datos de análisis) y `summaryGrid` (resumen persistente arriba de los tabs) no tenía consumidor en ningún task previo del backlog — incluidos en el alcance de "Dashboard" del título de esta task. `HeroBanner` puramente lee `heroStats` del context (slots `savingsTotal`/`payableTotal`/`receivableTotal` de #028/#029, nuevos `historicNet`/`historicMonths`/`expensePct`/`avgExpenses12` seteados acá por `AnalysisTab.load()`) — oculto si `!historicMonths`, mismo criterio que `heroBanner.style.display`. `updateHeroNetSavings()`/`updateHeroInvestment()` de `hero.js` NO se portan como funciones — su fórmula vive inline en `HeroBanner` (derivado directo del render, no hay DOM que actualizar imperativamente). `AnalysisTab` es lazy-on-tab-activate (prop `active`, mismo patrón que Savings/Debts #028/#029) — gap heredado: Hero banner/avgExpenses12 quedan en 0 hasta que el usuario visite el tab Analysis al menos una vez, igual que Net savings queda incompleto hasta visitar Savings/Debts (vanilla los carga todos eager vía `refreshAll()`, gap ya aceptado en tasks previas, no se corrigió acá por consistencia). `SummaryGrid` SÍ es eager (siempre montado, fetch reactivo a `[currentMonth]`) — vive fuera de cualquier tab, visible siempre igual que vanilla. `Layout.jsx`: `HeroBanner` arriba del month-bar, `SummaryGrid` entre month-bar y `TabNav` (mismo orden que `index.html`), `tab-analysis` ahora renderiza `AnalysisTab`. Tab "Compare" sigue sin contenido — no está en el alcance de ningún task del backlog, gap preexistente fuera de esta task. Verificado: `npm run build` limpio, `npm test -- --run` sin regresión (mismos 2 fallos preexistentes de `.env.local`/puerto). No se pudo probar render interactivo en browser ni las gráficas Chart.js reales (sin herramienta de browser en este entorno) — solo build + lógica revisada. (2026-08-13)
- [x] [P] [react][030] **Dominio Settings** — `src/components/SettingsModal.jsx` (único componente, sin submodulo separado — porta `settingsModal`/`openSettings()`/`closeSettings()`/`loadBackground()`/`applyBackground()`/`uploadBackground()`/`removeBackground()`/`onDimChange()` de `settings.js` 1:1). Estado `open` sube a `Layout.jsx` (`settingsOpen`, botón gear del header antes sin handler desde #022 lo dispara ahora); `bgUrl`/`dim` propios del modal, `useEffect` aplica `document.body.style.backgroundImage`/`--bg-dim`/clase `has-bg` como side-effect DOM directo — necesario porque el fondo es a nivel `<body>`, fuera del árbol React, mismo approach imperativo que vanilla, sin duplicar en un wrapper. Carga inicial de `/api/settings/background` en mount del modal (siempre montado en `Layout`, visibilidad por `open ? ... : null`) — replica `loadBackground()` de `loadApp()`, corre una vez sin esperar que se abra el modal, así el fondo se ve incluso antes de tocar el gear (igual que vanilla). Slider de dim y botón Remove solo se muestran con `bgUrl` seteado — replica `bgDimGroup`/`bgRemoveBtn` con `display:none` condicional de vanilla. Verificado: `npm run build` limpio, `npm test -- --run` sin regresión (mismos 2 fallos preexistentes de `.env.local`/puerto). No se pudo probar render interactivo en browser (sin herramienta de browser en este entorno) — solo build + lógica revisada. (2026-08-13)
- [x] [P] [react][029] **Dominio Debts** — `src/debts/DebtsTab.jsx` (único componente, sin fila separada — solo card "Add debt" + secciones receivable/payable con settle/delete inline, porta `debtsCard`/`addDebt()`/`settleDebt()`/`deleteDebt()` de `debts.js` 1:1) — fetch lazy on tab-activate vía prop `active`, mismo patrón que Savings/Family (#027/#028), sin filtro por mes (`/api/debts` no lleva `?month=`, fiel a vanilla). `deleteDebt` SÍ tiene `confirm('Delete this entry?')`, preservado exacto. `heroStats.payableTotal`/`heroStats.receivableTotal` (slots ya existentes en `AppStateContext` desde #020, sin consumidor hasta ahora) actualizados en cada `load()` con el total de ítems abiertos (`!settled`) por tipo — replica `state.heroReceivableTotal`/`state.heroPayableTotal` de vanilla; `updateHeroNetSavings()` NO se porta acá — combina también `heroStats.savingsTotal` (#028), consumidor real es el Hero banner de #031, mismo patrón de context-slot-antes-que-consumidor. `Layout.jsx`: `tab-savings` ahora renderiza `SavingsTab` + `DebtsTab` juntos (mismo tab-section que vanilla, restructurado mínimamente igual que el split de #024/#026/#028). Verificado: `npm run build` limpio, `npm test -- --run` sin regresión (mismos 2 fallos preexistentes de `.env.local`/puerto). No se pudo probar render interactivo en browser (sin herramienta de browser en este entorno) — solo build + lógica revisada. (2026-08-13)
- [x] [P] [react][028] **Dominio Savings** — `src/savings/`: `SavingsTab.jsx` (porta card "Add savings account"/`loadSavings()`/`addSavingsAccount()` de `savings.js` — fetch on-activate vía prop `active`+`[currentMonth]`, mismo patrón lazy que #023/#027; card "Balances this month" oculta cuando `!accounts.length`, fila "Total" replicada con mismo cálculo `hasBalance`/`hasPrev`/delta), `SavingsRow.jsx` (porta fila individual — cuenta linkeada a Plaid muestra balance read-only + nombre de banco vinculado, cuenta no linkeada muestra input editable + `<select>` "Link to bank account..." si hay `tellerAccounts`; `saveBalance`/`linkSavingsAccount`/`unlinkSavingsAccount`/`deleteSavingsAccount` — `deleteSavingsAccount` SÍ tiene `confirm('Delete this account and all its balance history?')`, preservado exacto). `heroStats.savingsTotal` (slot ya existente en `AppStateContext` desde #020, sin consumidor hasta ahora) actualizado en cada `load()` — replica `state.heroSavingsTotal = ...` de vanilla; `updateHeroNetSavings()`/`updateHeroInvestment()` de `hero.js` NO se portan acá — combinan `heroPayableTotal`/`heroReceivableTotal` (Debts, #029) y `heroAvgExpenses12` (Analysis, #031), ninguno existe todavía en React, y el Hero banner que consume estos valores es #031 — sin consumidor real hasta esa task, mismo patrón de context-slot-antes-que-consumidor de #020. Fuera de alcance a propósito: card "Add debt"/`debtsCard` del mismo `tab-savings` en `index.html` — es `src/debts.js`, dominio de #029, se agrega ahí restructurando este tab-section mínimamente (mismo patrón de split que #024/#026 con transactions/recurring). `Layout.jsx`: `tab-savings` ahora renderiza `SavingsTab`. Verificado: `npm run build` limpio (mismo warning preexistente de chunk >500kB, no nuevo), `npm test -- --run` sin regresión (mismos 2 fallos preexistentes de `.env.local`/puerto). No se pudo probar render interactivo en browser (sin herramienta de browser en este entorno) — solo build + lógica revisada. (2026-08-12)
- [x] [P] [react][027] **Dominio Family Bank (Plaid)** — `src/family-bank/`: `FamilyTab.jsx` (porta `#tab-family`/`loadFamily()`/`saveFamilyNames()`/`saveRemittances()` de `family-bank.js` — cálculo 5%/28%/28%/14.67%×3 idéntico, fetch on-activate vía prop `active`+`[currentMonth]`, mismo patrón lazy-on-tab-switch que `ProductList` de #023 en vez del `loadFamilyMembers()` eager de `loadApp()`; edit-names toggle sin DOM `getElementById`, controlado por state), `BankTab.jsx`+`TellerReviewCard.jsx` (portan `#tab-bank`/`loadBankEnrollments()`/`loadBankBalances()`/`loadPendingTransactions()`/`openPlaidLink()`/`triggerManualSync()`/`addTellerTransaction()`/`dismissTellerTransaction()` — `disconnectEnrollment` SÍ tiene `confirm(`Disconnect ${name}? Already-added transactions are kept.`)`, preservado exacto; mensaje de "Transactions to review" es un solo state derivado `pendingMessage` que se pisa a sí mismo igual que el único nodo DOM `#bankSyncStatus` de vanilla — "Checking banks..." al disparar sync manual, sobreescrito por el resultado real de `loadPending()` a los 30s, replica el `setTimeout` de vanilla tal cual; categorías de review usan `useTxCategories()`+`txDefaultCategories`, mismo merge que #024/#026; `Plaid.create(...)` referencia el global del script CDN ya cargado en `index.html`/`react-app.html`, sin import). Badge de pendientes (`#bankBadge` en `TabNav`) dejó de ser el placeholder DOM-manipulado por `updateBankBadge()`/`refreshBankBadge()` de vanilla (leftover de #022 sin consumidor real) — ahora `TabNav` recibe prop `bankPendingCount`, `Layout.jsx` la levanta con fetch inicial a `/api/teller/pending-count` (replica el `refreshBankBadge()` de `loadApp()`, corre una vez al montar sin esperar que se visite el tab) y la actualiza vía callback `onPendingCountChange` que `BankTab` dispara en cada `loadPending()`/add/dismiss. Fuera de alcance a propósito: `triggerManualSync()` en vanilla también llama `loadSavings()` al final del `setTimeout` — Savings (#028) aún no está portado a React, se omite esa llamada hasta que exista equivalente; agregar transacción bancaria (`addTellerTransaction`) en vanilla dispara `loadTransactions()`/`loadSummary()` global — en React, `TransactionList`/`TransactionsTab` no se enteran (viven en otro tab-section, siempre montados sin token de refresh compartido entre dominios) — gap documentado, no se agregó plumbing cross-tab nuevo por YAGN. Verificado: `npm run build` limpio (mismo warning preexistente de chunk >500kB, no nuevo), `npm test -- --run` sin regresión (mismos 2 fallos preexistentes de `.env.local`/puerto). No se pudo probar render interactivo en browser, ni flujo real de Plaid Link (requiere credenciales sandbox) — solo build + lógica revisada. (2026-08-12)
- [x] [P] [react][026] **Dominio Recurring/Fixed items** — `src/transactions/`: `RecurringForm.jsx` (porta card "Add fixed income or expense"/`addRecurringTransaction()`/`updateRecCategories()` de `recurring.js` — reusa `useTxCategories()` solo pa refrescar `dbTxCategories` post-alta vía `loadCategories()`, igual que `loadTransactionCategories()` de vanilla; reusa el `datalist#txCategoryList` ya propio de `TransactionForm.jsx` en vez de duplicarlo, vía `list="txCategoryList"` — válido porque `TransactionForm` sigue montado bajo el mismo patrón always-mounted de #023/#025), `RecurringList.jsx`+`RecurringRow.jsx` (portan `loadRecurringTransactions()`/`toggleEditRecurring()`/`saveRecurringTransaction()`/`deleteRecurringTransaction()` — `deleteRecurringTransaction` SÍ tiene `confirm('Stop adding this automatically every month? Already-added transactions are kept.')`, preservado exacto; a diferencia de `deleteTransaction` de #024 que no lo tiene). `RecurringList` sin dependencia de mes (`useEffect([refreshToken])` nomás) — a diferencia de `TransactionList`, la plantilla recurring no es per-mes, replica que `loadRecurringTransactions()` en vanilla tampoco filtra por `state.currentMonth`. `TransactionsTab.jsx`: agrega `RecurringForm`+`RecurringList` bajo `TransactionForm`+`TransactionList`, mismo orden que `index.html`; `onSaved` de `RecurringForm` sube tanto su propio `recurringRefreshToken` como el `refreshToken` de `TransactionList` — replica que `addRecurringTransaction()` en vanilla llama `refreshAll()` (el backend auto-genera una transacción normal del mes actual desde la plantilla nueva, hay que reflejarla en la lista de transacciones); `saveRecurringTransaction`/`deleteRecurringTransaction` NO llaman `refreshAll()` en vanilla, solo recargan su propia lista — replicado igual (`RecurringRow` solo llama `onChanged` local, no toca `refreshToken` de transacciones). Verificado: `npm run build` limpio, `npm test -- --run` sin regresión (mismos 2 fallos preexistentes de `.env.local`/puerto). No se pudo probar render interactivo en browser (sin herramienta de browser en este entorno) — solo build + lógica revisada. (2026-08-12)
- [x] [P] [react][025] **Dominio Receipts** — `src/receipts/`: `ReceiptForm.jsx` (porta `#form-receipt`/`addReceipt()`/scan OCR de `receipts.js`), `ReceiptLine.jsx` (porta línea de ítem con detección de producto existente via `findProduct`, cálculo inline de unit price desde qty/amount/count actuales en vez de replicar el mapa persistente `state.rlLastUnitPrice` de vanilla — verificado equivalente contra `onRLSearch`/`onRLQtyInput`/`onRLCountInput`/`onRLAmtInput`/`updateRLPrice`), `ReceiptList.jsx`+`ReceiptGroup.jsx`+`ReceiptItemRow.jsx` (portan `loadReceipts()`/edit-inline de receipt y de ítem individual — `updateEditRIPrice` muestra `'—'` cuando qty/amount inválidos, NO cae a `item.unit_price`, fiel a vanilla línea 407; `deleteReceipt` SÍ tiene `confirm()`, a diferencia de `deleteTransaction` de #024). `src/products/PurchasesTab.jsx` nuevo: contenedor que reemplaza el `PurchaseForm` suelto de #023 en `tab-purchases` — mode-toggle Purchase/Receipt/Scan (`#btn-mode-purchase`/`#btn-mode-receipt`/`#btn-scan-receipt` de `index.html`), `PurchaseForm` y `ReceiptForm` ambos siempre montados, visibilidad por `style={{display}}` en vez de mount/unmount — necesario pa que `scanInputRef` (input file oculto dentro de `ReceiptForm`) siga clickeable desde el botón "Scan receipt" del padre sin importar el modo activo, mismo patrón que `setMode()` de vanilla (nunca saca el form del DOM). `ReceiptList` se renderiza siempre bajo ambos forms, fetch reactivo a `[currentMonth, refreshToken]` (mismo patrón de #024). `Layout.jsx`: `tab-purchases` ahora renderiza `PurchasesTab` en vez de `PurchaseForm` directo. `datalist#productList`/`#storeList` (de `PurchaseForm.jsx`, #023) reusados cross-componente vía `list=`, válido porque `PurchaseForm` sigue montado bajo el patrón always-mounted. Verificado: `npm run build` limpio, `npm test -- --run` sin regresión (mismos 2 fallos preexistentes de `.env.local`/puerto). No se pudo probar render interactivo en browser (sin herramienta de browser en este entorno) — solo build + lógica revisada. (2026-08-12)
- [x] [P] [react][024] **Dominio Transactions** — `src/transactions/`: `TransactionForm.jsx` (porta card "Add income or expense"/`addTransaction()`/`updateTxCategories()` de `core.js` — categorías derivadas de `txDefaultCategories[type]` ∪ `dbTxCategories[type]` del context `useTxCategories`, igual que vanilla; cambiar `type` resetea `category` como hacía `updateTxCategories()` al limpiar `#txCategory`), `TransactionList.jsx`+`TransactionRow.jsx` (portan card "Income & expenses this month"/`loadTransactions()`/`toggleEditTransaction()`/`saveTransaction()`/`deleteTransaction()` — `deleteTransaction` SIN `confirm()`, fiel a vanilla que tampoco lo tiene ahí a diferencia de `deletePurchase`/`deleteProduct`). `TransactionList` fetch keyed en `useEffect([currentMonth, refreshToken])` — reactivo al cambio de mes en vez del `refreshAll()` imperativo de vanilla (establece patrón pa futuros dominios money-por-mes: savings/debts/analysis). `TransactionsTab.jsx` conecta form+list vía `refreshToken` (state local, sube en `onSaved`) ya que son hermanos sin dominio compartido en context. `Layout.jsx`: placeholder `tab-transactions` ahora renderiza `TransactionsTab`. Fuera de alcance a propósito: sección "Add fixed income or expense"/`recurringListCard` del mismo tab-section en `index.html` — es `src/recurring.js`, dominio de #026, se agrega ahí restructurando este tab-section mínimamente (igual patrón que #023/#025 con purchases/receipts). Verificado: `npm run build` limpio, `npm test -- --run` sin regresión (mismos 2 fallos preexistentes de `.env.local`/puerto). No se pudo probar render interactivo en browser (sin herramienta de browser en este entorno) — solo build + lógica revisada. (2026-08-12)
- [x] [P] [react][023] **Dominio Products/Purchases** — `src/products/`: `PurchaseForm.jsx` (porta `#form-purchase`/`addPurchase()`/`onProductSearch()`/`toggleNewProduct()`/`updateUnitPrice()`/`createProduct()` de `core.js` — producto existente detectado con `findProduct` sobre `allProducts` del context, auto-rellena `amount` con `last_unit_price × qty`; producto nuevo usa `CategorySelect`/`ProductTypeSelect`/`UnitSelect` de #021; fetch inicial `GET /api/products`+`GET /api/stores` al montar, igual que `loadApp()`), `ProductList.jsx`+`ProductRow.jsx`+`PurchaseHistoryRow.jsx` (portan `#tab-products`/`loadProductsTab()`: agrupación categoría→tipo→producto vía `useMemo`, búsqueda cliente, historial expandible, edit/delete de producto y de compra individual — reemplaza el DOM-string-building + `classList.toggle('visible')` de vanilla por mount/unmount condicional, confirmado seguro porque `styles.css` no tiene selectores por ID en esta sección, solo clases `.pt-*`/`.edit-form`/`.new-product-fields`). `ProductList` recibe prop `active` (`activeTab === 'products'`) y solo hace `GET /api/product-history` cuando se activa/reactiva la tab — replica el trigger de `switchTab('products')` en vanilla en vez de cargar al montar `Layout`, ya que `loadApp()`/`refreshAll()` tampoco incluyen `loadProductsTab()`. `Layout.jsx`: los placeholders `tab-purchases`/`tab-products` ahora renderizan `PurchaseForm`/`ProductList`, resto de tabs sigue vacío (dominios #024-030). Fuera de alcance a propósito: mode-toggle Purchase/Receipt/Scan de `#tab-purchases` (la mitad "Receipt" es #025 — se agrega ahí, restructurando este mismo tab-section mínimamente), refresh del selector de mes tras alta de compra en mes nuevo (`Layout` solo carga `/api/months` una vez al montar — gap menor, mes actual ya siempre incluido), refresh en vivo del datalist de stores tras nueva compra en `ProductList` (solo se refresca en `PurchaseForm`). Categorías: `CategorySelect` sigue siendo lista estática `DEFAULT_PRODUCT_CATEGORIES` (decisión de #021, no reincorpora categorías custom de `/api/product-categories` que vanilla soportaba vía datalist). Verificado: `npm run build` limpio, `npm test -- --run` sin regresión (mismos 2 fallos preexistentes de `.env.local`/puerto). No se pudo probar render interactivo en browser (sin herramienta de browser en este entorno) — solo build + lógica revisada. (2026-08-12)
- [x] [S] [react][022] **Layout base + navegación por tabs** — `src/App.jsx` ahora es switch real por `useAuth().status` (`loading`→null, `anon`→`AuthGate`, resto→`Layout`), reemplaza el smoke-test de task 021. `src/components/AuthGate.jsx`: porta `#authGate` de `index.html` 1:1 (mismas clases `auth-gate`/`card auth-box`/`form-group`/`btn-primary`/`auth-toggle-link`), inputs controlados, usa `useAuth()` (`authMode`, `error`, `toggleAuthMode`, `submitAuth`) — ya no hay funciones globales `toggleAuthMode()`/`submitAuth()` tocando el DOM, es autocontenido. `src/components/TabNav.jsx`: array `TABS` (8 tabs, mismo orden/labels que `index.html`: Purchases/Income & Expenses/Products/Compare/Savings/Analysis/Family/Bank Sync), exporta `TAB_IDS` pa que `Layout` genere los `tab-section` placeholders. `src/Layout.jsx`: header (título/subtitle, botón logout via `useAuth().logout`, botón settings placeholder sin handler — modal real es #030), month-bar (`<select>` controlado por `useCurrentMonth()`, opciones desde `GET /api/months` via `apiFetch`, incluye mes actual si el backend no lo trae — misma lógica que `loadMonths()` de `core.js`), `TabNav` + 8 divs `tab-section` vacíos (`id="tab-<name>"`, toggle `active` vía estado `activeTab` en memoria — mismo contrato CSS que vanilla: `.tab-section{display:none}`/`.active{display:block}`, confirmado en `public/styles.css`, todos montados simultáneo así que no hay flicker/refetch al cambiar de tab). Fuera de alcance a propósito (quedan pa tasks futuras): contenido real de cada tab-section (#023-030), `summaryGrid`/`heroBanner` (ligados a `loadSummary`/#031, ni mencionados en el título de esta task), side-effect loaders de `switchTab` (bank/family/products data fetches — se enganchan en los dominios respectivos), modal de settings (#030). `index.html`/`src/core.js` vanilla NO se tocan, siguen vivos hasta cutover (#034). Verificado: `npm run build` limpio, `npm test -- --run` sin regresión (mismos 2 fallos preexistentes de `.env.local`/puerto, ajenos a esta task). No se pudo probar render interactivo en browser (sin herramienta de browser en este entorno) — solo build + lógica revisada. (2026-08-12)
- [x] [S] [react][021] **UI helpers compartidos** — `src/components/`: `CategorySelect`, `UnitSelect`, `ProductTypeSelect` (controlados, reemplazan `categoryInput`/`unitOptions`/`populateTypeSelect`+`typeInputHtml` de `src/ui-helpers.js`). `ProductTypeSelect` absorbe también `onRLTypeChange`/`onRLCategoryChange` (antes funciones globales que tocaban el DOM directo): ahora es autocontenido, resuelve el sentinel `__new__` internamente y expone al padre un solo `value` string final (tipo existente o nuevo) — el re-filtrado de tipos al cambiar de categoría ya no necesita wiring manual, es reactivo vía prop `category`. `src/lib/findProduct.js`: función pura `findProduct(products, name)`, ya no importa `state.js` — recibe el array por parámetro. `src/ui-helpers.js` NO se toca, sigue vivo pa vanilla (`core.js`, `receipts.js`) hasta cutover (#034). Sin consumidores reales todavía (los dominios que los usan — Products/Purchases #023, Receipts #025 — no existen aún), por diseño según dependencia declarada en el backlog. `App.jsx` smoke-test monta los tres. Verificado: `npm run build` limpio, suite vanilla sin regresión. No se pudo probar render interactivo en browser (sin herramienta de browser en este entorno) — solo build + lógica revisada. (2026-08-12)

- [x] [S] [react][020] **Estado global → Context/hooks** — `src/state/AppStateContext.jsx` nuevo: `AppStateProvider` + hooks por dominio (`useCurrentMonth`, `useProducts`, `useFamilyMembers`, `useTxCategories`, `usePlaidSyncData`, `useHeroStats`), reemplaza el objeto mutable `state` de `src/state.js` para el lado React (sin Redux, un solo context + `useState` por slice). `currentMonth` persiste a `localStorage` igual que vanilla. `src/state.js` NO se toca — sigue vivo pa vanilla (10 consumidores) hasta cutover (#034); sus constantes puras (`DEFAULT_PRODUCT_CATEGORIES`, `UNITS`, `txDefaultCategories`) son reusables tal cual desde React, no requieren context. Deliberadamente fuera: `isNewProduct`/`rlCounter`/`lastKnownUnitPrice`/`rlLastUnitPrice` (estado local de un form, no global — vive en el componente de #023) y `chartInstances` (refs imperativos de Chart.js — `useRef` local en #031, no context). `main.jsx` envuelve `<App/>` en `<AppStateProvider>` (dentro de `AuthProvider`). Verificado: `npm run build` limpio, suite vanilla sin regresión. (2026-08-12)

- [x] [S] [react][019] **Auth context/hook** — `src/AuthContext.jsx` nuevo: `AuthProvider`/`useAuth` (login/signup/logout/status/user/error), reemplaza `src/auth.js` para el lado React. `src/api.js` decuplado de `auth.js`/DOM: se quitó el import directo de `showAuthGate` y el `authToken()`/`localStorage` vestigial (sesión es cookie httpOnly de Better-Auth, ver ADR 004) — ahora expone `setUnauthorizedHandler(fn)`, registrado tanto por `auth.js` (vanilla, sigue mostrando el auth gate en 401) como por `AuthProvider` (React, resetea a `status:'anon'`). `main.jsx` envuelve `<App/>` en `<AuthProvider>`; `App.jsx` placeholder ahora consume `useAuth()` pa smoke-test. Verificado: `npm run build` limpio (ambas entradas), suite vanilla sin regresión (mismos 2 fallos preexistentes de `.env.local`, no nuevos). (2026-08-12)

- [x] [S] [react][018] **Scaffold React 19 + Vite** — `react@19`/`react-dom@19` + `@vitejs/plugin-react@^4` (v6 pedía Vite 8, incompatible con Vite 6 actual, pineado a v4). `react-app.html` nuevo (entrada Vite multi-page paralela a `index.html`, sin tocar el vanilla que sigue sirviendo) con `src/main.jsx`/`App.jsx` placeholder — se retira en cutover (#034). `vite.config.js`: `plugins:[react()]` + `build.rollupOptions.input` con ambas entradas. Sin `react-router-dom` ni state library — YAGNI, ver ADR 005. Verificado: `npm run build` limpio (ambas entradas en `dist/`), `npm run dev` sirve HMR real en `/react-app.html`, suite vanilla intacta (2 fallos preexistentes en `api.test.js` por `.env.local` con puerto distinto al hardcodeado en el test, no relacionado a este scaffold). (2026-08-12)

- [x] [S] [app][017] **Fix historic net / avg-12 / investment funds no recalculaban al cambiar mes** — `loadAnalysis()` en `src/analysis.js` filtraba meses contra `new Date()` (fecha real de hoy) en vez de `state.currentMonth` (mes seleccionado en selector top). `heroNet`, avg últimos 12 e investment funds (via `heroAvgExpenses12` → `updateHeroInvestment()`) quedaban fijos sin importar el mes elegido. Corregido: ahora filtra meses `<= state.currentMonth`, excluyendo el seleccionado (mismo criterio que antes excluía "hoy"). Bug relacionado en `finances-backend`: `/api/months` (selector top) listaba meses con solo entrada manual en `monthly_summaries` sin transacciones reales — corregido del lado backend, `/api/analysis` sigue incluyendo esos meses manuales aparte (correcto, es su propósito). (2026-08-06)
- [x] [S] [app][016] **Mostrar mensaje real de `sync_error` en Bank Sync** — backend cambió `sync_error` de booleano a string real (rate-limited/auto-retry, login-required, o error crudo de Plaid). Frontend mostraba "Connection lost — reconnect" pa cualquier caso, incluyendo rate limit de Plaid que se autolimpia con backoff de 4h en backend — reconectar en ese caso quemaba una sesión de Plaid Link sin necesidad. Ahora renderiza el mensaje real, estilo rojo solo cuando es login-required. (2026-08-05)
- [x] [S] [app][015] **Fixed/recurring income & expense items + persistir mes seleccionado** — nueva sección "Fixed items" en tab Transactions (`src/recurring.js`): CRUD contra `/api/recurring-transactions` (backend ya implementado). Backend auto-genera una transacción normal cada mes desde plantillas activas; editar/eliminar la instancia de un mes no toca la plantilla, solo borrar la plantilla detiene generación futura. Además: mes seleccionado ahora persiste en `localStorage` (`currentMonth`), ya no resetea al mes actual en cada reload. Ver `docs/adr/004-auth-cookie-session.md` pa el ADR relacionado de auth (mismo día, distinto tema). (2026-08-05)
- [x] [S] [app][014] **Fix categoría rígida en form de bank sync** — `family-bank.js` usaba `<select>` de categorías predefinidas en "agregar transacción bancaria" (a diferencia del form normal de transacciones, que usa `input`+`datalist` con texto libre). Cambiado al mismo patrón — permite categoría nueva. (2026-08-05)
- [x] [S] [app][013] **Auth: JWT bearer → sesión por cookie** — `finances-api` migró a Better-Auth (sesión por cookie, CORS `credentials: true`). `src/api.js` actualizado: `apiFetch` ya no inyecta header `Authorization` desde `localStorage`, ahora manda `credentials: 'include'`. Ver `docs/adr/004-auth-cookie-session.md`. Probado en local contra backend en puerto 3002 vía `.env.local` (gitignored — `.gitignore` no tenía patrón `.env*` antes de esta sesión, corregido). (2026-08-05)
- [x] [S] [app][012] **Fix "transacciones pendientes" reaparecían tras Skip/Add en Bank Sync** — causa raíz en backend (`plaid.service.ts`), no en frontend — confirmado no era bug de este mundo. (2026-08-05)

- [x] [S] [ops][010] **Migrar repo a github.com/Ledder-Dev** — usuario confirmó repo público. Repo nuevo `Ledder-Dev/finances-frontend` creado con historia completa (`master`+`development` pusheados), repo viejo `camachoeng/finances-frontend` intacto como remote secundario `camachoeng`, default branch puesto a `master`. Deploy vía GitHub Pages intentado y revertido después (ver #011) — la migración del repo en sí queda cerrada y en uso. (2026-08-04)
- [x] [S] [ops][005] **Reescribir historia git — descartada** — GitGuardian detectó `UMAMI_PASSWORD` y `PAGESPEED_API_KEY` expuestos en texto plano en `opencode.json`, commit `70ab062`. Rotación de ambas credenciales: hecha por compañero (2026-07-30). Reescritura de historia (`git filter-repo` + force-push a `origin/master` y `origin/development`, ambas ramas comparten el commit) descartada por decisión del usuario — secretos ya rotados, riesgo/beneficio de reescribir no lo justifica. `opencode.json` y `.mcp.json` ya en `.gitignore` para evitar recurrencia. Cerrado como decisión final, no pendiente. (2026-08-01)
- [x] [S] [app][009] **Fix pantalla negra en LAN móvil** — `auth.init()` en `src/auth.js` no tenía try/catch: si `apiFetch('/api/auth/me')` fallaba (backend inalcanzable desde móvil por LAN), la promesa rechazaba sin manejo, ni `showAuthGate()` ni `hideAuthGate()` corrían, y quedaba visible el fondo oscuro (`--bg-page`) sin gate ni container — pantalla negra. Agregado try/catch: en error de red se loguea y se muestra el auth gate. Causa raíz de por qué el backend es inalcanzable desde móvil (host bind, firewall, CORS) queda por confirmar del lado de `finances-backend` — fuera de este mundo. (2026-07-31)
- [x] [S] [app][008] **ADR 002 modularización ES modules** — `docs/adr/002-modularizacion-es-modules.md` documenta decisión ya ejecutada (#001), alternativas descartadas y consecuencias. Cuenta huérfana Teller confirmada limpia por el usuario. (2026-07-31)
- [x] [S] [app][007] **Merge `.claude/CLAUDE.md` en `CLAUDE.md`** — trigger `/graphify` movido a sección `## graphify` de `CLAUDE.md` raíz del mundo; `.claude/CLAUDE.md` eliminado (redundante). (2026-07-30)
- [x] [S] [app][006] **Fix link cuenta bancaria en Savings tab** — `src/savings.js` comparaba `a.teller_account_id` (campo viejo, ya no existe en response de `/api/savings`) contra `t.account_id` de `/api/teller/balances`; backend renombró el campo a `plaid_account_id` al migrar de Teller a Plaid. Balance sí sincronizaba pero UI nunca mostraba "Auto — banco" tras enlazar. Corregido: comparación ahora usa `a.plaid_account_id`. Verificado en navegador real por el usuario — las 3 cuentas enlazadas (Ive's Checking, Ive's Savings, My Savings) muestran estado "Auto" tras el fix. También verificado en sesión: conexión Plaid real funciona end-to-end (create-link-token → Link modal → exchange-token → balances/enrollments cargan tras "Check now"). Cuenta vieja huérfana de config Teller anterior ("Customized Cash Rewards Visa Signature") sigue apareciendo — pendiente que el usuario pruebe botón Disconnect; si no limpia, es dato huérfano en DB de `finances-backend`, fuera de alcance de este mundo. (2026-07-30)
- [x] [P] [app][003] **README.md** — expandido: requisitos, variables de entorno (`VITE_API_BASE_URL`), desarrollo, build/preview, testing, estructura completa de `src/*.js` y nota de deploy. (2026-07-29)
- [x] [P] [app][002] **Agregar tests** — Vitest + jsdom instalados, `npm test` corre `vitest run`. Alcance: funciones puras/aisladas extraídas en #001 — `trimmedMean` (analysis), `unitOptions`/`categoryInput`/`typeInputHtml`/`findProduct` (ui-helpers), resolución de `API_BASE` con/sin `VITE_API_BASE_URL` y `apiFetch` (inyección de header Authorization), 17 tests. NO se persigue cobertura de flujos de render/DOM completos (fuera de proporción para SPA sin capa de dominio separada). (2026-07-29)
- [x] [S] [app][004] **Config API_BASE por env var** — `import.meta.env.VITE_API_BASE_URL` con fallback a heurística de hostname; `.env.example` agregado. (2026-07-29)
- [x] [S] [app][001] **Modularizar app.js** — dividido en `src/*.js` (ES modules bundleados por Vite): `state`, `api`, `auth`, `core`, `receipts`, `savings`, `debts`, `analysis`, `family-bank`, `settings`, `ui-helpers`, `hero`, `main`. Verificado con `npm run build` limpio (15 módulos, sin warnings) + auditoría estática cruzando los 65 nombres de función usados en `onclick`/`oninput`/`onchange`/`onblur`/`onfocus` (incluyendo los generados dinámicamente vía `innerHTML`) contra las funciones exportadas — cero huecos. Smoke test visual confirmado por el usuario en navegador real, con `finances-backend` levantado (auth gate carga sin errores de consola). (2026-07-29)
